'use server'

export async function handleSearch(city: string, sport: string) {
//zuvor console.log(city..);
    if (!city && !sport) {
        return {error: 'City or Sport invalid/missing.'}
    }

    async function create() {
    }

    return (

    )
}
